[pytest]
DJANGO_SETTINGS_MODULE = stock_project.settings_test
python_files = tests.py test_*.py *_tests.py
addopts = --tb=short --strict-markers
markers =
    integration: tests that hit external services (yfinance) — slow, skip by default
filterwarnings =
    ignore::DeprecationWarning
